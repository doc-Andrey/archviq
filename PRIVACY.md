# Privacy

The current ARCHVIQ build performs the free profile calculation in the running application session.

The app does not contain code that writes names or exact dates of birth to a persistent database.

The browser cognitive battery runs locally in the page and downloads its result as a CSV file to the user's device.

If persistent accounts, analytics or report storage are added later, this notice should be updated before deployment.
